<div class="nav-main">
  <div class="container">
    <nav class="navbar navbar-expand-lg">
      <div class="container-fluid">
        <a class="navbar-brand todolist-nav" href="/" data-turbolinks="false">Todolist</a>
        <% if user_signed_in? %>
          <% if defined?(@task) %>
          <a class="app-nav-link open-popup" data-popup="popup2">Create new task</a>
          <% end %>
        <div class="dropdown-account">
          <a href="<%= edit_user_registration_path %>" class="fas fa-user user-icon dropbtn" data-turbolinks="false"></a>
          <div class="dropdown-account-content">
            <%= link_to 'Sign out', destroy_user_session_path, class: 'link-style', method: :delete %>
          </div>
        </div>
        <% else %>
        <a href="<%= new_user_session_path %>" class="btn btn-outline-light log-in">Log In</a>
        <a href="<%= new_user_registration_path %>" class="btn btn-outline-light sign-up">Sign Up</a>
        <% end %>
      </div>
    </nav>
  </div>
</div>
