<% if @category != nil %>
<h3><%= @category.name %></h3>
<%= link_to 'Delete category', category_path(@category), data: {method: :delete, confirm: 'Are you sure?'}, class: 'cat-controls' %>
<% end %>
