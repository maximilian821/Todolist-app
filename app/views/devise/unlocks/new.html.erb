<div class="container devise-container">
  <div class="devise-content">
    <h2 class="text-center mb-3">Resend unlock instructions</h2>

    <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="field">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, class: 'form-control mb-2', autocomplete: "email" %>
      </div>

      <div class="actions">
        <%= render "devise/shared/links" %>
      </div>

      <div class="actions">
        <%= f.submit "Resend unlock instructions", class: 'btn btn-outline-light btn-action mt-2' %>
      </div>
    <% end %>
  </div>
</div>
